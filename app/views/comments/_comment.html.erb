<div class="comment_container">
  <% @user = User.find(comment.user_id) %>
  <% @post = Post.find(comment.post_id)%>
  <%= image_tag @user.avatar, :width=>50, :crop=>"fill", class:"img-circle img-thumbnail"%>
  <span class="username"><%= @user.username %> : </span>
  <%= comment.content %>
  <% if current_user == @user%>
    <%= link_to image_tag('edit.png',class:"icon"), edit_post_comment_path(@post.id, comment.id) %>
  <% end %>
  <%= link_to image_tag('delete.png',class:"icon"), post_comment_path(@post.id,comment.id), method: :delete, data: { confirm: 'Are you sure?' }%>
</div>
